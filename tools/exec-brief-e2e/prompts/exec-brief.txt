CRITICAL EXECUTION RULES (read first, obey strictly):
- This is a real execution task verified by inspecting inference-router HTTP logs. Your work is ONLY counted if the router actually sees the corresponding tool call. Prose claims do not count. Fabricated URLs do not count. Made-up image paths do not count.
- Your VERY FIRST action MUST be a tool call (azureclaw_discover, azureclaw_spawn, or foundry_web_search). Do not emit any natural-language text until after you have made at least three tool calls.
- Telegram is OPTIONAL. If the telegram tool is not registered, silently skip Telegram steps and continue with mesh + foundry work.
- Required tool calls that MUST be observed by the router before you may write the brief — verification fails otherwise:
    (a) foundry_web_search that returns at least 6 distinct https:// URLs. You will cite those URLs VERBATIM in footnotes. Writing "[REDACTED_URL]", "[link]", "(source)" or any other placeholder is FABRICATION and fails verification — re-run foundry_web_search until you have 6 real URLs.
    (b) foundry_image_generation with model "gpt-image-1" and size "1024x1024" — actually call this tool; do not invent a hero image path.
    (c) foundry_code_execute that renders a matplotlib bar chart PNG — actually call this tool; do not fake an image link.
    (d) at least 3 azureclaw_mesh_send calls, covering each pair analyst↔viz, analyst↔writer, viz↔writer.
    (e) at least 1 MCP DeepWiki call (read_wiki_structure / read_wiki_contents / ask_question).
- Only after (a)–(e) are satisfied may you emit prose. The final reply MUST be the full assembled brief (hero image markdown + chart markdown + ~900 words with footnoted real URLs), nothing else.
- "I'll orchestrate…" / "I will now…" preambles are forbidden. Just call the tools.

Peer roster (single source of truth — use these exact names with azureclaw_mesh_send / azureclaw_mesh_transfer_file, never invent variants, never self-send):
  analyst — research analyst, runs first
  viz     — data-viz engineer, runs after analyst
  writer  — senior technical writer, assembles the final brief

EXECUTION STRATEGY:
You may do the research, image generation, code execution, and MCP work directly in the parent — sub-agents are unreliable and adding mesh hops to that path tends to drop artifacts. Do (a) foundry_web_search, (b) foundry_image_generation (gpt-image-1, 1024x1024), (c) foundry_code_execute (matplotlib chart), and (e) ≥1 MCP DeepWiki call yourself.

Required tool sequence — execute IN THIS EXACT ORDER before writing any prose:
  1. foundry_web_search (≥3 distinct queries, ≥6 distinct https:// URLs total)
  2. mcp_deepwiki call (read_wiki_structure or read_wiki_contents on at least 2 platforms)
  3. foundry_image_generation (model="gpt-image-1", size="1024x1024", hero image)
  4. foundry_code_execute (Python + matplotlib, render the 4-platform metrics scorecard as a grouped bar chart; save to /mnt/data/scorecard.png; the wrapper auto-downloads to /sandbox/.openclaw/workspace/scorecard.png). Without an actual foundry_code_execute call the run FAILS verification — referencing a fabricated chart path will not pass. The chart markdown in the brief must use the path the tool returned in `result.files[0].path`, never an invented path.
  5. azureclaw_spawn + azureclaw_mesh_send (mesh-relay-demo below) — AFTER the four foundry tool calls above are done.

For (d), the sibling-pair mesh demo, run this MESH RELAY DEMO once the brief content is collected but BEFORE you emit the final brief:
- azureclaw_spawn analyst, viz, writer if azureclaw_discover shows them missing.
- azureclaw_mesh_send a short instruction message to "analyst" saying: "ack-relay: please azureclaw_mesh_send the string 'analyst-to-viz' to viz AND the string 'analyst-to-writer' to writer, then reply ok".
- azureclaw_mesh_send to "viz" saying: "ack-relay: please azureclaw_mesh_send the string 'viz-to-writer' to writer, then reply ok".
- azureclaw_mesh_send to "writer" saying: "ack-relay: please reply 'ok' to confirm receipt".
- azureclaw_mesh_await up to 60s for each of the three replies. Don't block the brief if they don't all return — the on-wire evidence is captured by the router whether or not the sub-agents reply.

Then emit the final brief verbatim.

I want a 2-page executive brief on the state of agentic AI sandboxing in 2026, with a hero image and a real chart. Use three sub-agents that talk to each other directly via the AGT mesh. There is a dedicated mesh messaging API and a mesh file-transfer API available to each agent. Throughout the run, post short status pings to the Telegram channel I have open so I can watch progress on my phone. Make sure every agent is properly instructed to use Foundry tools when it executes its work — Foundry image generation for image work, Foundry web search for research, and Foundry code execution for the chart. For every file transfer use the mesh file transfer tool.

Sub-agent 1 — analyst
System prompt: "You are a research analyst. Use foundry_web_search to find material published in 2026 on agentic sandbox runtimes (AzureClaw, OpenAI Sandboxes, Anthropic Computer Use isolation, Google Project Astra runtime, AWS Bedrock AgentCore, Cloudflare AI Sandbox, AKS Confidential Containers for AI). For at least TWO of those platforms, follow up with the DeepWiki MCP (tools: read_wiki_structure, read_wiki_contents, ask_question) to pull structured project docs — citation must include both the foundry_web_search URL and the deepwiki ref so the trace shows real MCP traffic. Produce a structured JSON artifact with: (1) trends: 5–8 key trends, (2) attack-surface categories: 3 named attack-surface categories with citations, (3) platforms: comparison table of ≥4 platforms × 4 columns {isolation_primitive, egress_model, attestation_story, governance_integration}, (4) metrics: a 4-platform numeric scorecard (0–10) for each of the four columns — this drives the chart downstream. Cite every claim with URL + publication date. No prose narrative."

Sub-agent 2 — viz
System prompt: "You are a data-viz engineer. You will receive analyst's JSON artifact directly over AGT. Use the Foundry code execution skill to run a Python snippet (matplotlib, no seaborn) that renders the metrics scorecard as a 4-platform grouped bar chart, exporting a 1024×640 PNG. Then use the Foundry image generation skill (gpt-image-1) to produce a 1024×1024 hero image titled 'Agentic Sandboxes 2026 — Defense in Depth' in the visual style of an isometric data-center cutaway with subtle Azure cyan accents. Hand BOTH images plus analyst's artifact directly to writer over AGT (base64-inline or as attached blobs using azureclaw_mesh_transfer_file — your choice, just confirm in the message which is which). It might take some time for the analyst to finish, so periodically check your mesh inbox to receive the json."

Sub-agent 3 — writer
System prompt: "You are a senior technical writer. You will receive analyst's JSON artifact and viz's two images directly over AGT. Produce a 2-page executive brief: hero image at the top, opening hook (≤80 words), 3 numbered sections (Trends / Attack-Surface / Platform Landscape with the bar chart inline), and a 'So what for AzureClaw?' closing paragraph. Preserve every citation as footnotes. Reject and ask the upstream sibling for clarification — over AGT — if any claim is uncited, any platform row has gaps, the chart is unreadable, or the hero image is off-brief. It might take some time, so periodically check your mesh inbox to receive the json and the two images."

Coordination protocol (must verify on the wire):
1. Check first whether the three sub-agents already exist using azureclaw_discover. If they exist, verify mesh connectivity with a single ping each; otherwise spawn all three sub-agents in parallel. Analyst runs first, then fans out to viz and writer.
2. analyst → viz:    JSON artifact, AGT direct using azureclaw_mesh_transfer_file.
3. analyst → writer: same JSON artifact, AGT direct using azureclaw_mesh_transfer_file.
4. viz → writer:     chart PNG + hero PNG, AGT direct using azureclaw_mesh_transfer_file.
5. Any rejection / clarification rounds also stay sibling-to-sibling.
6. Only the final assembled brief (with images embedded) comes back to me.

Telegram status thread (use the telegram channel plugin). Post one terse update at each milestone — keep them under 240 chars each:
  - "🔍 analyst: searching 2026 sources…"
  - "📊 analyst done — N sources, M platforms scored. Handing to viz + writer."
  - "🎨 viz: chart rendered (Foundry code-exec ✓), hero image generating…"
  - "🖼️ viz done — both images sent to writer via AGT."
  - "✍️ writer: assembling brief…"
  - "✅ Brief ready. AGT sibling msgs: <count>. Total turns: <count>."
On any failure, a single "❌ <agent>: <one-line cause>" instead of the success messages from that point onward.

Acceptance checks (confirm each before finalizing):
- **≥6 distinct 2026 sources cited**, with ≥1 MCP-sourced (DeepWiki) reference visible in the trace.
- metrics scorecard has all 16 cells populated; bar chart legend names match the platform list.
- Hero image is 1024×1024 and was produced by gpt-image-1 (not described, actually generated).
- Chart was rendered by Foundry code-exec (not ASCII-faked) — show the tool-call evidence in your reply.
- Relay log shows ≥3 distinct sibling pairs of E2E-encrypted messages: analyst→viz, analyst→writer, viz→writer (KNOCK + ratchet).
- Telegram thread received at least 5 status messages in order.
- Final brief fits 2 pages (~900 words excluding footnotes), with hero on top and chart inline in §3.

Go.

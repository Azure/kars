You orchestrate three sub-agents to produce a 2-page executive brief on the state of agentic AI runtime environments in 2026. Your role is orchestration only — do not call foundry tools or MCP tools yourself.

Step 1. Call azureclaw_discover('*') as your first action.

Step 2. For each of analyst, viz, writer that is missing, call azureclaw_spawn with role text ('research analyst', 'data visualisation engineer', 'technical writer') and governance=true.

Step 3. Dispatch task briefs to each sub-agent with azureclaw_mesh_send (back to back, do not await between dispatches):

to_agent='analyst' message=
"Research brief: state of agentic AI runtime environments in 2026. Use foundry_web_search with ≥3 distinct queries to gather ≥6 https URLs covering leading agentic AI runtimes from major cloud and AI providers. For ≥2 of those runtimes, also call the execbrief-deepwiki MCP server (read_wiki_structure / read_wiki_contents / ask_question). Build a JSON artifact with: trends (5–8), control_categories (3 named with citations), runtimes (≥4 × 4 cols: workload_boundary, network_posture, attestation_story, governance_integration), metrics (4-runtime 0–10 scorecard across those 4 cols). Cite each claim with URL + date. Save via foundry_code_execute to /mnt/data/analyst.json then azureclaw_mesh_transfer_file to BOTH viz AND writer. Sparse first-query results are normal; broaden queries. Do not report blocked unless ≥3 query variants AND your inbox AND azureclaw_discover('*') all turn up nothing."

to_agent='viz' message=
"You will receive analyst.json from analyst over the mesh. First call azureclaw_mesh_await(senders=['analyst'], timeout_seconds=600). When the file arrives: (1) foundry_code_execute with matplotlib to load /sandbox/.openclaw/workspace/incoming/analyst.json and render the metrics scorecard as a 4-runtime grouped bar chart, exporting /mnt/data/scorecard.png at 1024x640; (2) foundry_image_generation model='gpt-image-1' size='1024x1024' for a hero image titled 'Agentic AI Runtimes 2026 — Defense in Depth' (isometric data-center cutaway, Azure cyan accents); (3) azureclaw_mesh_transfer_file three times to to_agent='writer': scorecard.png, the hero PNG, and analyst.json."

to_agent='writer' message=
"You will receive analyst.json and two PNGs from analyst+viz. First call azureclaw_mesh_await(senders=['analyst','viz'], timeout_seconds=900). Then write a 2-page brief on the state of agentic AI runtime environments in 2026: hero image markdown at top, ≤80-word hook, 3 numbered sections (Trends / Control Domains / Runtime Landscape with the chart inline), and a 'So what for AzureClaw?' closing (~900 words excluding footnotes). Preserve every analyst citation as numbered footnotes with verbatim https URLs (no placeholders). file_write to /sandbox/.openclaw/workspace/brief.md then azureclaw_mesh_transfer_file(to_agent='parent', file_path='/sandbox/.openclaw/workspace/brief.md', description='final brief'). Also a short azureclaw_mesh_send to parent: 'final brief delivered'."

Step 4. Call azureclaw_mesh_await(senders=['writer'], timeout_seconds=900). Sub-agents exchange artifacts directly over the mesh; you are not in the data path.

Step 5. Read writer's delivered file (/sandbox/.openclaw/workspace/incoming/brief.md) and emit its contents as your final reply.

If a sub-agent replies with status=blocked, re-dispatch its brief once. If it blocks twice, surface the reason and stop — do not substitute your own work. If azureclaw_mesh_await times out, run azureclaw_discover and azureclaw_spawn_status to diagnose, then surface status and stop.

Go.
